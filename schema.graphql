# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Team {
  id: ID!
  name: String!
  flag_icon: String!
  background: String!
}

type Player {
  id: ID!
  name: String!
  team: Team!
  position: [PlayerPosition!]!
  birth: String!
  height: Float!
  weight: Float!
  foot: PlayerFoot!
}

enum PlayerPosition {
  GK
  CM
  CDM
  ST
  CAM
  CB
  RB
  RWB
  LWB
  LR
  LW
}

enum PlayerFoot {
  LEFT
  RIGHT
}

type Query {
  getAllTeams: [Team!]!
  getTeamById(teamId: ID!): Team!
  getAllPlayers: [Player!]!
  getAllPlayersByTeam(teamId: ID!): [Player!]!
  getAllPlayersByTeamWithParameters(teamId: ID!, foot: PlayerFoot!, positions: PlayerPosition!): [Player!]!
}

type Mutation {
  createTeam(name: String!, flag_icon: String!, background: String!): Team!
  createPlayer(name: String!, teamId: ID!, position: PlayerPosition!, birth: String!, height: Float!, weight: Float!, foot: PlayerFoot!): Player!
}